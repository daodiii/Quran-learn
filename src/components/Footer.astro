---
const currentYear = new Date().getFullYear();
---

<footer class="footer" style="padding-bottom: env(safe-area-inset-bottom);">
  <div class="footer-container">
    <div class="footer-grid">
      <!-- Brand Column -->
      <div class="footer-brand">
        <a href="/" class="brand-link">
          <svg class="brand-icon" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="16" cy="16" r="14" stroke="currentColor" stroke-width="2"/>
            <path d="M16 7 L16 25 M11 11 Q16 5 21 11 M11 21 Q16 27 21 21" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
          <span class="brand-name">Quranic Arabic</span>
        </a>
        <p class="brand-description">
          A comprehensive resource for learning Quranic Arabic grammar.
        </p>
      </div>

      <!-- Navigation Links -->
      <div class="footer-section">
        <h2 class="footer-heading">Navigation</h2>
        <ul class="footer-links">
          <li><a href="/learn/">Learn</a></li>
          <li><a href="/surahs/">Surahs</a></li>
          <li><a href="/resources/">Resources</a></li>
          <li><a href="/about/">About / Feedback</a></li>
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="footer-bottom">
      <p class="footer-copyright">&copy; {currentYear} Quranic Arabic</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    margin-block-start: auto;
    background: var(--bg-primary);
    color: #ffffff;
    border-top: 3px solid var(--accent-gold);
    padding-block: var(--spacing-3xl);
  }

  :global([data-theme="light"]) .footer {
    background: #F5F1EB;
    color: #1A1147;
    border-top: 3px solid var(--accent-gold);
  }

  .footer-container {
    max-inline-size: 90rem;
    margin: 0 auto;
    padding-inline: 2rem;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  @media (min-width: 768px) {
    .footer-grid {
      grid-template-columns: 2fr 1fr;
    }
  }

  /* Brand */
  .footer-brand {
    max-inline-size: 400px;
  }

  .brand-link {
    display: flex;
    align-items: center;
    gap: 1rem;
    text-decoration: none;
    margin-block-end: 1.5rem;
  }

  .brand-icon {
    inline-size: 40px;
    block-size: 40px;
    color: var(--accent-gold);
  }

  .brand-name {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 600;
    color: #ffffff;
    letter-spacing: -0.01em;
  }

  :global([data-theme="light"]) .brand-name {
    color: #1A1147;
  }

  .brand-description {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.8;
  }

  :global([data-theme="light"]) .brand-description {
    color: #6B5B7E;
  }

  /* Sections */
  .footer-heading {
    font-family: var(--font-display);
    font-size: 0.8125rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--accent-gold);
    margin-block-end: 1.5rem;
  }

  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .footer-links a {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    transition: all var(--transition-fast);
  }

  :global([data-theme="light"]) .footer-links a {
    color: #6B5B7E;
  }

  .footer-links a:hover {
    color: var(--accent-gold);
    transform: translateX(4px);
  }

  /* Bottom Bar */
  .footer-bottom {
    margin-block-start: 4rem;
    padding-block-start: 2rem;
    border-top: 2px solid var(--accent-gold);
    text-align: center;
    opacity: 0.6;
  }

  .footer-copyright {
    font-family: var(--font-display);
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.5);
  }

  :global([data-theme="light"]) .footer-copyright {
    color: #6B5B7E;
  }
</style>
