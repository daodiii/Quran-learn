---
---

<div class="announcement-ticker" role="region" aria-label="Site announcements">
  <div class="ticker-container">
    <div class="ticker-messages">
      <p class="ticker-message ticker-message-1">
        Check out the Resources section — there's a lot of helpful material to support your learning
      </p>
      <p class="ticker-message ticker-message-2">
        Have feedback or suggestions? Let us know — we're always looking to improve
      </p>
      <p class="ticker-message ticker-message-3">
        If this site has been helpful, please share it with others who might benefit
      </p>
    </div>
  </div>
</div>

<style>
  .announcement-ticker {
    width: 100%;
    height: 40px;
    background: rgba(251, 247, 237, 0.95);
    border-bottom: 1px solid rgba(184, 134, 11, 0.15);
    position: relative;
    z-index: 40;
    overflow: hidden;
  }

  :global([data-theme="dark"]) .announcement-ticker {
    background: rgba(45, 40, 24, 0.95);
    border-bottom: 1px solid rgba(242, 217, 140, 0.15);
  }

  .ticker-container {
    max-width: 90rem;
    margin: 0 auto;
    padding-inline: 2rem;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ticker-messages {
    position: relative;
    height: 1.4em;
    width: 100%;
    text-align: center;
  }

  .ticker-message {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    margin: 0;
    font-family: var(--font-display, 'Cormorant', serif);
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--text-secondary, #3D2C5E);
    line-height: 1.4;
    white-space: nowrap;
    opacity: 0;
    animation: slideMessage 18s ease-in-out infinite;
  }

  .ticker-message-1 {
    animation-delay: 0s;
  }

  .ticker-message-2 {
    animation-delay: 6s;
  }

  .ticker-message-3 {
    animation-delay: 12s;
  }

  @keyframes slideMessage {
    0% {
      opacity: 0;
      transform: translateY(8px);
    }
    5% {
      opacity: 1;
      transform: translateY(0);
    }
    28% {
      opacity: 1;
      transform: translateY(0);
    }
    33% {
      opacity: 0;
      transform: translateY(-8px);
    }
    100% {
      opacity: 0;
      transform: translateY(-8px);
    }
  }

  @media (max-width: 640px) {
    .ticker-message {
      font-size: 0.8125rem;
      white-space: normal;
    }

    .announcement-ticker {
      height: 44px;
    }

    .ticker-messages {
      height: 2.8em;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ticker-message {
      animation-name: fadeMessage;
    }
  }

  @keyframes fadeMessage {
    0% {
      opacity: 0;
    }
    5% {
      opacity: 1;
    }
    28% {
      opacity: 1;
    }
    33% {
      opacity: 0;
    }
    100% {
      opacity: 0;
    }
  }
</style>
